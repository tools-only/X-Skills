# 📚 SKILL_EVOLUTION.md - 技能进化追踪

> **最后更新**: 2026-01-13
> **Context 占用**: ~8,000 tokens
> **用途**: 追踪编码能力发展和学习进度

---

## 🎯 技能进化框架

### 核心理念

**持续改进 = 错误减少 + 模式识别 + 能力提升**

```
初级阶段 → 中级阶段 → 高级阶段 → 专家阶段
   ↓           ↓           ↓           ↓
错误频发   错误减少    主动预防    零错误设计
```

---

## 📊 技能发展阶段

### 阶段 1: 初级（Beginner）

**特征**:
- 频繁触发高频错误（E001-E010）
- 需要查阅文档才能使用工具
- 依赖自动修复和重试
- 代码审查发现多个问题

**典型错误模式**:
```typescript
// ❌ 串行异步（E001）
await fetchUser()
await fetchOrders()
await fetchProducts()

// ❌ 无超时轮询（E002）
while (true) {
  const status = await checkStatus()
  if (status === 'done') break
  await sleep(1000)
}

// ❌ 错误不重新抛出（E003）
try {
  await processData()
} catch (error) {
  console.error(error)  // 只记录，不抛出
}
```

**改进目标**:
- 学习 Top 10 高频错误
- 熟悉基础工具（bytebase, chart, /commit）
- 建立错误自检习惯

**时间跨度**: 2-4 周

---

### 阶段 2: 中级（Intermediate）

**特征**:
- 高频错误减少 50%+
- 能够独立使用主要工具（MCP, Skills）
- 开始主动使用自检清单
- 代码审查问题减少

**改进模式**:
```typescript
// ✅ 并行异步（E001 已掌握）
const [users, orders, products] = await Promise.all([
  fetchUsers(),
  fetchOrders(),
  fetchProducts()
])

// ✅ 带超时轮询（E002 已掌握）
const maxAttempts = 60
for (let i = 0; i < maxAttempts; i++) {
  const status = await checkStatus()
  if (status === 'done') break
  await sleep(1000)
}

// ✅ 错误正确处理（E003 已掌握）
try {
  await processData()
} catch (error) {
  console.error('Failed to process:', error)
  throw error  // 重新抛出
}
```

**新的关注点**:
- SQL 优化（CTE, 避免重复计算）
- 工具组合使用（bytebase → chart → report）
- 性能分析和优化

**改进目标**:
- E001-E005 错误率 < 20%
- 独立完成标准任务
- 理解工具决策树

**时间跨度**: 4-8 周

---

### 阶段 3: 高级（Advanced）

**特征**:
- 高频错误率 < 10%
- 主动预防错误（编码前自检）
- 能够优化现有代码
- 熟练使用 Plugins 组合

**主动预防示例**:
```typescript
// 编码前自检清单（已内化）
// ✅ 异步并行？
// ✅ 超时控制？
// ✅ 错误处理？
// ✅ 资源清理？
// ✅ SQL 优化？

// 直接写出优化版本
async function processBatch(items: Item[]) {
  const maxAttempts = 60
  const timeout = 5000

  try {
    // 并行 + 超时 + 错误处理
    const results = await Promise.race([
      Promise.allSettled(
        items.map(item => processItem(item))
      ),
      new Promise((_, reject) =>
        setTimeout(() => reject(new Error('Timeout')), timeout)
      )
    ])

    return results
  } catch (error) {
    console.error('Batch processing failed:', error)
    await cleanupResources()  // 资源清理
    throw error  // 重新抛出
  }
}
```

**SQL 优化能力**:
```sql
-- 使用 CTE 预过滤（E004 已精通）
WITH filtered_orders AS (
  SELECT * FROM orders
  WHERE created_at >= '2026-01-01'
),
user_stats AS (
  SELECT
    user_id,
    COUNT(*) as order_count,
    SUM(amount) as total_amount
  FROM filtered_orders
  GROUP BY user_id
)
SELECT * FROM user_stats WHERE order_count > 5
```

**改进目标**:
- E001-E010 错误率 < 5%
- 独立设计数据分析流程
- 理解所有工具的最佳实践

**时间跨度**: 8-16 周

---

### 阶段 4: 专家（Expert）

**特征**:
- 零高频错误（E001-E010）
- 代码审查零问题
- 能够设计复杂工作流
- 贡献最佳实践和模式

**设计能力**:
```typescript
// 设计完整的数据分析工作流
async function performCompleteAnalysis(params: AnalysisParams) {
  // 1. 并行查询多个数据源
  const [orderData, costData, metricData] = await Promise.all([
    queryOrders(params.dateRange),
    queryCosts(params.dateRange),
    queryMetrics(params.dateRange)
  ])

  // 2. 数据转换和聚合
  const aggregated = aggregateData([orderData, costData, metricData])

  // 3. 并行生成多个图表
  const [trendChart, distributionChart, comparisonChart] = await Promise.all([
    generateTrendChart(aggregated.trends),
    generateDistributionChart(aggregated.distribution),
    generateComparisonChart(aggregated.comparison)
  ])

  // 4. 生成综合报告
  const report = await generateReport({
    data: aggregated,
    charts: [trendChart, distributionChart, comparisonChart],
    insights: analyzeInsights(aggregated)
  })

  return report
}
```

**贡献示例**:
- 发现新的错误模式 → 更新 ERROR_CATALOG.md
- 优化工作流程 → 更新 workflows/
- 创建新的自检清单 → 更新 BEST_PRACTICES.md

**改进目标**:
- 维持零错误率
- 指导他人避免常见错误
- 创新工作流和最佳实践

**时间跨度**: 16+ 周

---

## 📈 学习曲线指标

### 1. 错误频率跟踪

**衡量方法**: 每周触发的 Top 10 错误次数

| 阶段 | E001 | E002 | E003 | E004 | E005 | E006-E010 | 总计 |
|------|------|------|------|------|------|-----------|------|
| 初级（Week 1-4） | 8 | 6 | 5 | 4 | 3 | 10 | 36 |
| 中级（Week 5-12） | 2 | 2 | 1 | 2 | 1 | 5 | 13 |
| 高级（Week 13-24） | 0 | 0 | 0 | 1 | 0 | 2 | 3 |
| 专家（Week 25+） | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

**改进趋势**:
```
错误次数
40 ┤●
35 ┤ ╲
30 ┤  ╲
25 ┤   ╲
20 ┤    ●
15 ┤     ╲
10 ┤      ╲
 5 ┤       ●
 0 ┤        ●━━━━━━━━━━━●
   └─────────────────────────
   Week 1  4   8   12  16  20  24
```

---

### 2. 代码质量分数

**衡量方法**: /code-review 评分（0-100 分）

| 维度 | 初级 | 中级 | 高级 | 专家 |
|------|------|------|------|------|
| 正确性 | 65 | 80 | 92 | 98 |
| 安全性 | 60 | 75 | 88 | 95 |
| 性能 | 55 | 70 | 85 | 92 |
| 可维护性 | 60 | 75 | 88 | 95 |
| **总分** | **60** | **75** | **88** | **95** |

**质量改进曲线**:
```
分数
100┤                       ●
 90┤                   ●
 80┤           ●
 70┤
 60┤   ●
 50┤
   └─────────────────────────
   初级  中级  高级  专家
```

---

### 3. 工具熟练度

**衡量方法**: 独立完成任务的成功率

| 工具类别 | 初级 | 中级 | 高级 | 专家 |
|---------|------|------|------|------|
| MCP Servers | 40% | 75% | 95% | 100% |
| Skills | 50% | 80% | 95% | 100% |
| Plugins | 30% | 60% | 85% | 98% |
| 工具组合 | 20% | 50% | 80% | 95% |

**能力发展路径**:
```
初级: 单一工具 → bytebase 查询 → 输出数据
中级: 双工具组合 → bytebase 查询 → chart 生成
高级: 多工具链 → bytebase → 处理 → chart → 报告
专家: 复杂工作流 → 并行查询 → 聚合 → 多图表 → 洞察
```

---

### 4. 完成任务时间

**衡量方法**: 相同任务的完成时间（分钟）

| 任务类型 | 初级 | 中级 | 高级 | 专家 |
|---------|------|------|------|------|
| 简单查询 | 10 | 5 | 3 | 2 |
| 数据分析 | 45 | 25 | 15 | 10 |
| Bot 归因 | 90 | 45 | 25 | 15 |
| 完整报告 | 120 | 60 | 35 | 20 |

**效率提升**:
```
时间（分钟）
120┤●
100┤ ╲
 80┤  ╲
 60┤   ●
 40┤    ╲
 20┤     ●━━●
  0┤
    └─────────────────
    初级 中级 高级 专家
```

---

## 🎓 能力进阶路径

### 异步编程精通之路

#### Level 1: 基础（初级）
```typescript
// 能够使用基础 async/await
async function fetchData() {
  const data = await fetch('/api/data')
  return data.json()
}
```

#### Level 2: 并行（中级）
```typescript
// 能够并行执行独立任务
const [users, orders, products] = await Promise.all([
  fetchUsers(),
  fetchOrders(),
  fetchProducts()
])
```

#### Level 3: 超时控制（高级）
```typescript
// 能够添加超时控制
const result = await Promise.race([
  fetchData(),
  new Promise((_, reject) =>
    setTimeout(() => reject(new Error('Timeout')), 5000)
  )
])
```

#### Level 4: 错误恢复（专家）
```typescript
// 能够处理部分失败和重试
const results = await Promise.allSettled([
  retryWithBackoff(() => fetchUsers()),
  retryWithBackoff(() => fetchOrders()),
  retryWithBackoff(() => fetchProducts())
])

const succeeded = results.filter(r => r.status === 'fulfilled')
const failed = results.filter(r => r.status === 'rejected')

if (failed.length > 0) {
  console.warn(`${failed.length} operations failed`)
}
```

---

### SQL 优化精通之路

#### Level 1: 基础查询（初级）
```sql
SELECT * FROM orders
WHERE user_id = 123
```

#### Level 2: JOIN 和聚合（中级）
```sql
SELECT
  u.name,
  COUNT(o.id) as order_count,
  SUM(o.amount) as total_amount
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
GROUP BY u.id
```

#### Level 3: CTE 优化（高级）
```sql
WITH recent_orders AS (
  SELECT * FROM orders
  WHERE created_at >= '2026-01-01'  -- 预过滤
),
user_stats AS (
  SELECT
    user_id,
    COUNT(*) as order_count,
    SUM(amount) as total_amount
  FROM recent_orders
  GROUP BY user_id
)
SELECT * FROM user_stats WHERE order_count > 5
```

#### Level 4: 复杂分析（专家）
```sql
WITH order_window AS (
  SELECT * FROM orders
  WHERE created_at BETWEEN '2026-01-01' AND '2026-01-31'
),
task_window AS (
  SELECT
    user_id,
    bot_id,
    COUNT(*) as task_count
  FROM tasks
  WHERE created_at BETWEEN '2025-12-25' AND '2026-02-07'
  GROUP BY user_id, bot_id
),
attribution AS (
  SELECT
    t.bot_id,
    SUM(o.amount) as revenue,
    SUM(t.task_count) as tasks
  FROM order_window o
  JOIN task_window t ON o.user_id = t.user_id
  GROUP BY t.bot_id
)
SELECT
  a.*,
  ROUND(a.revenue / a.tasks, 2) as revenue_per_task
FROM attribution a
ORDER BY a.revenue DESC
```

---

### 工具使用精通之路

#### Level 1: 单一工具（初级）
```bash
# 只会查询数据
bytebase-execute_sql: "SELECT * FROM orders"
```

#### Level 2: 双工具组合（中级）
```bash
# 查询 → 图表
1. bytebase 查询数据
2. chart 生成图表
```

#### Level 3: 多工具链（高级）
```bash
# 查询 → 处理 → 图表 → 报告
1. bytebase 查询
2. 数据转换
3. chart 生成多个图表
4. content-writer 生成报告
```

#### Level 4: 复杂工作流（专家）
```bash
# 并行查询 → 聚合 → 多图表 → 洞察分析
1. 并行执行：bytebase, honeycomb, statsig
2. 数据聚合和交叉分析
3. 并行生成：5 种图表
4. 自动洞察提取
5. 生成完整分析报告
```

---

## 🏆 成功指标

### 短期目标（1-2 个月）

| 指标 | 初始 | 目标 | 达成 |
|------|------|------|------|
| E001-E003 错误率 | 80% | < 20% | ☐ |
| 代码质量分数 | 60 | 75+ | ☐ |
| 独立完成任务率 | 40% | 70%+ | ☐ |
| 工具使用熟练度 | 30% | 60%+ | ☐ |

### 中期目标（3-6 个月）

| 指标 | 初始 | 目标 | 达成 |
|------|------|------|------|
| E001-E010 错误率 | 20% | < 5% | ☐ |
| 代码质量分数 | 75 | 88+ | ☐ |
| 独立完成任务率 | 70% | 90%+ | ☐ |
| 任务完成时间 | 45min | 25min | ☐ |

### 长期目标（6-12 个月）

| 指标 | 初始 | 目标 | 达成 |
|------|------|------|------|
| E001-E010 错误率 | 5% | 0% | ☐ |
| 代码质量分数 | 88 | 95+ | ☐ |
| 独立完成任务率 | 90% | 98%+ | ☐ |
| 任务完成时间 | 25min | 15min | ☐ |

---

## 📝 进化追踪模板

### 每周技能自评

**周次**: Week __
**日期**: 2026-__-__ 至 2026-__-__

#### 1. 错误统计

| 错误 ID | 触发次数 | 较上周 | 备注 |
|---------|---------|--------|------|
| E001 | ___ | ±___ | ___________ |
| E002 | ___ | ±___ | ___________ |
| E003 | ___ | ±___ | ___________ |
| E004 | ___ | ±___ | ___________ |
| E005 | ___ | ±___ | ___________ |
| 其他 | ___ | ±___ | ___________ |
| **总计** | ___ | ±___ | ___________ |

#### 2. 完成任务记录

| 任务类型 | 次数 | 成功率 | 平均时间 |
|---------|------|--------|---------|
| 简单查询 | ___ | ___% | ___min |
| 数据分析 | ___ | ___% | ___min |
| Bot 归因 | ___ | ___% | ___min |
| 完整报告 | ___ | ___% | ___min |

#### 3. 新学技能

- [ ] _________________ (描述新掌握的技能或模式)
- [ ] _________________ (描述新掌握的技能或模式)
- [ ] _________________ (描述新掌握的技能或模式)

#### 4. 改进重点

下周重点改进的方向：
1. _________________
2. _________________
3. _________________

#### 5. 代码质量

本周代码审查分数: ____ / 100

主要问题:
- _________________
- _________________

---

## 🔗 相关文档

### 错误预防
- [ERROR_CATALOG.md](../errors/ERROR_CATALOG.md) - 错误知识库总览
- [system-errors/](../errors/system-errors/) - 系统级错误详解

### 最佳实践
- [BEST_PRACTICES.md](../references/BEST_PRACTICES.md) - 编码最佳实践
- [CAPABILITY_MATRIX.md](../references/CAPABILITY_MATRIX.md) - 能力选择矩阵

### 工作流程
- [DATA_ANALYSIS.md](../workflows/DATA_ANALYSIS.md) - 数据分析工作流
- [AUTO_EXECUTION.md](../workflows/AUTO_EXECUTION.md) - 自动执行模式

### 核心配置
- [CLAUDE.md](../CLAUDE.md) - 主配置文档
- [DECISION_TREE.md](../DECISION_TREE.md) - 能力决策树

---

## 💡 使用建议

### 新手必读（Week 1-4）
1. ✅ 阅读 [ERROR_CATALOG.md](../errors/ERROR_CATALOG.md) Top 10 错误
2. ✅ 每次编码前使用自检清单
3. ✅ 每周填写技能自评模板
4. ✅ 关注错误频率趋势

### 进阶学习（Week 5-12）
1. ✅ 深入学习每个错误模式的正确做法
2. ✅ 练习工具组合使用
3. ✅ 开始优化现有代码
4. ✅ 追踪代码质量分数

### 高级优化（Week 13-24）
1. ✅ 主动预防错误，而非被动修复
2. ✅ 设计复杂工作流
3. ✅ 贡献新的最佳实践
4. ✅ 指导他人避免常见错误

### 专家之路（Week 25+）
1. ✅ 维持零错误率
2. ✅ 创新工作流和模式
3. ✅ 更新文档和知识库
4. ✅ 建立个人最佳实践库

---

**📌 记住**:
- 技能进化是渐进的过程
- 每周小改进积累成大飞跃
- 持续追踪是成功的关键
- 错误是最好的学习机会
