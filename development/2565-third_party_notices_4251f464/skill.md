# THIRD_PARTY_NOTICES

This repository's code and pipeline are MIT-licensed, but each third-party skill keeps its original license.
Do not assume a skill is MIT unless its metadata explicitly marks it as compatible.

Regenerate this file with:

```bash
python scripts/check_metadata_compliance.py \
  --skills-dir skills \
  --metadata-schema schema/metadata.schema.json \
  --notices THIRD_PARTY_NOTICES.md
```

## Summary

- Metadata entries scanned: **(generated at runtime)**
- Notice rows generated: **(generated at runtime)**
- Compatible entries: **(generated at runtime)**
- Restricted entries: **(generated at runtime)**

## Entries

Generated output will list skill-level attribution rows.
